[project]
name = "powerpoint-agent-backend"
version = "0.1.0"
description = "Simple PowerPoint alignment agent"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "anthropic>=0.40.0",
    "pydantic>=2.0.0",
]

[tool.uv]
dev-dependencies = []